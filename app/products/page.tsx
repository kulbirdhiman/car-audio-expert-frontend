import ListCards from "@/components/product/ListCards";
const Page = () => {
  return (
    // <Suspense fallback={<div>Loading products...</div>}>
      <ListCards />
    // </Suspense>
  );
};

export default Page;
